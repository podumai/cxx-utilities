name: windows-latest
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  windows-latest-build:
    uses: ./.github/workflows/ci.yml
    with:
      os: windows-latest
      compiler: cl
