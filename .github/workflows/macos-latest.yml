name: macos-latest
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  macos-latest-build:
    uses: ./.github/workflows/ci.yml
    with:
      os: macos-latest
      compiler: apple-clang
